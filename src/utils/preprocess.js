/*
  Placeholder for future AI/preprocessing hooks.
  Export functions to apply noise reduction, SSIM filters, or OpenCV transforms
  to images before comparison.
*/

module.exports = {
  // Example hook: (imgBuffer) => modifiedBuffer
  noop: function(buffer) { return buffer; }
};
